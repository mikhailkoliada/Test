name: test

on: [push]

jobs:
  upload-lwc-converage:
    name: Test
    runs-on: macos-12

    steps:
      - uses: actions/checkout@v3
      - name: test
        run: |
          gcc-11 --version
          gcc-11 hello.c -o hello
          ./hello


          

