name: test

on: [push]

jobs:
  build:

    runs-on: windows-latest

    steps:
      - name: call
        run: |
          $crudeList = ghcup list -t ghc -r
          $crideList.split(' ')[2]
        
