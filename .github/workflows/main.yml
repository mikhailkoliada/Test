name: test

on: [push]

jobs:
  upload-lwc-converage:
    name: Test
    runs-on: windows-2019

    steps:
      - name: test
        run: |
          [System.Text.Encoding]::Default
        shell: pwsh


          

