name: test

on: [push]

jobs:
  upload-lwc-converage:
    name: Test
    runs-on: windows-2022

    steps:
      - name: test
        run: |
          [System.Text.Encoding]::Default
          wslconfig.exe /list
        shell: pwsh


          

