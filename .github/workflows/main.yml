name: test

on: [push]

jobs:
  upload-lwc-converage:
    name: Test
    runs-on: ubuntu-latest

    steps:
      - name: test
        run: |
          whoami
          id
          sudo getent group ntp


          

