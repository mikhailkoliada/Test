name: test

on: [push]

jobs:
  upload-lwc-converage:
    name: Test
    runs-on: windows-2022

    steps:
      - uses: actions/checkout@v3
      - name: test
        run: |
          gcc -V
          gcc hello.c -o hello
          ./hello


          

