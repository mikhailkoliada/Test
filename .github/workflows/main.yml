name: test

on: [push]

jobs:
  upload-lwc-converage:
    name: Test
    runs-on: windows-2022

    steps:
      - name: test
        run: |
          C:\msys64\usr\bin\bash -lc "pacman --noconfirm -S mingw-w64-ucrt-x86_64-gcc"
          ls C:\msys64\ucrt64\bin


          

